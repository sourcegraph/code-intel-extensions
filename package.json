{
  "license": "MIT",
  "scripts": {
    "prettier": "prettier '**/*.{js?(on),ts,yml,md}' --write --list-different",
    "prettier-check": "npm run prettier -- --write=false"
  },
  "devDependencies": {
    "@sourcegraph/prettierrc": "^2.2.0",
    "chokidar-cli-infanticide": "^1.3.1",
    "concurrently": "^4.1.0",
    "prettier": "^1.16.4"
  }
}
